

<section class="box">
<div id="photo_produit">
<%= image_tag(@product.picture_url, :width => 450, :height => 350) if @product.picture.present? %>
</div>

<div class="col-lg-12">
  <strong>I want to exchange: </strong>
 <%= @product.name.capitalize %>
 <br>
</div>

<div class="col-lg-12">
  <strong>Description:</strong>
  <%= @product.description.capitalize %>
</div>

<div class="col-lg-12">
  <strong>Looking for:</strong>
  <%= @product.iteminexchange.capitalize %>
</div>
</br>
<div class="col-lg-offset-9 col-lg-3">
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
</div>
</section>

<section class="box2">
<h2>Minichat</h2>
<% @product.comments.each do |comment| %>
  <p>
    <strong>Your name</strong>
    <%= comment.commenter %>
  </p>
 
  <p>
    <strong>Comment:</strong>
    <%= comment.text %>
  </p>
 
<h2>Add a message</h2>
<% @product.comments.each do |f| %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>
 
  <p>
    <strong>Comment:</strong>
    <%= comment.text %>
  </p>
<% end %>
<%= form_for([@product, @product.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
</section>
<% end %>

